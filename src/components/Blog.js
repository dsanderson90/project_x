import React from "react"
import Display from "../components/Display"
import Fade from "react-reveal/Fade"

export const Blog = () => (
    <Fade top>
    <Display>
    <h1>Blog</h1>
    </Display>
    </Fade>
)
